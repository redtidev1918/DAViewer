# 0.5.2

## 本次更新

- 修复推荐页下拉刷新：列表不在顶部时，单次下拉即可触发刷新，不再需要先上滑到顶再下拉一次。

- Fixes pull-to-refresh on the recommendation feed: a single pull-down now refreshes even when the list is scrolled away from the top, instead of requiring a scroll-up before the refresh works.
